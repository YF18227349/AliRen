layui.define([], function (exports) {
    let $ = layui.jquery,
        fun = layui.fun,
        _val = null;
    let body = $('body'), device = fun.initWebSite();
    fun.ready(() => {
    });
    exports('web', {});
});